export module mint: interpreter;

import std;
import xxas;

import :cpu;
import :memory;
import :context;
import :stackframe;
import :semantics;
import :binding;
import :operand;
import :instruction;
import :jit_compiler;

namespace mint
{
    namespace interpreter
    {

    };

    struct Interpreter
    {
        
    };
};

